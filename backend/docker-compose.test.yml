version: '3.8'

services:
  test_db:
    image: postgres:15-alpine
    container_name: ideaforge_test_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ideaforge_test
    ports:
      - "5432:5432"
